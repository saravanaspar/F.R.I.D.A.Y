# Desktop and browser control

See [CUA integration](CUA.md). The active computer-use plugin is Cua Driver, configured in `friday.config.json`.

The legacy Computer control plane and Linux X11 provider remain in the repository for migration of project targets and desktop lease clients. They are not registered in the standard runtime. Do not use the legacy `friday setup computer` path for new installs.
