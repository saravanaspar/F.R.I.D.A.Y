[project]
name = "python-skill"
version = "0.1.0"
dependencies = ["example-runtime"]

[project.scripts]
python_skill = "python_skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/python_skill"]
